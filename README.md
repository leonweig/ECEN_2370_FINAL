The Single Player version uses the touch display and you play against the AI.
The Multiplayer version uses the gyro. If the gyro gets bad data because of shaking the device, leave it on the table for 2 seconds and it resets. (fast tilting to left and right is better)
